name: 'ecommerce_analytics'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_duckdb'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
